#!/bin/bash

# [ -d "/opt/app-root/src/cra" ] && exit 0

set -e

shopt -s dotglob

fix-permissions ./

echo "---> check  /tmp/src ..."
ls -l /tmp/src

# Fix source directory permissions
fix-permissions ./

