import React from 'react';

import IdentificationExamplesPage from '../pages/IdentificationExamples';

export default {
  title: 'ITVR/IdentificationExamples',
  component: IdentificationExamplesPage
};

const Template = (args) => <IdentificationExamplesPage {...args} />;
export const Default = Template.bind({});
