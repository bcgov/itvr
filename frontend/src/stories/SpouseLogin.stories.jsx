import React from 'react';

import SpouseLogin from '../components/SpouseLogin';

export default {
  title: 'ITVR/SpouseLogin',
  component: SpouseLogin
};

const Template = (args) => <SpouseLogin {...args} />;
export const Default = Template.bind({});
