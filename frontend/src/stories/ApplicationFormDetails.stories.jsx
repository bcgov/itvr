import React from 'react';

import ApplicationFormDetails from '../components/ApplicationFormDetails';

export default {
  title: 'ITVR/ApplicationFormDetails',
  component: ApplicationFormDetails
};

const Template = (args) => <ApplicationFormDetails {...args} />;
export const Default = Template.bind({});
