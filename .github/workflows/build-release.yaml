
name: build-release

env:
  PR_NUMBER: 285

on:
  push:
    branches: [ GHA-manual-approval-1.10.0 ]
  workflow_dispatch:
  workflow_call:

jobs:
  build:
    name: Build on Openshift through BCDK pipeline
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:

      ## Ask for approval for deploying on ITVR Test
      - name: Manual Workflow Approval
        uses: trstringer/manual-approval@v1.6.0
        with:
          secret: ${{ github.TOKEN }}
          approvers: kuanfandevops
          minimum-approvals: 1
          issue-title: "Deploying GHA-manual-approval-1.10.0 to ITVR Test"

      ## it will checkout to /home/runner/work/itvr/itvr
      - name: Check out repository
        uses: actions/checkout@v2

      - name: Log in to OpenShift
        uses: redhat-actions/oc-login@v1
        with:
          openshift_server_url: ${{ secrets.OPENSHIFT_SERVER }}
          openshift_token: ${{ secrets.OPENSHIFT_TOKEN }}
          insecure_skip_tls_verify: true
          namespace: ${{ secrets.OPENSHIFT_NAMESPACE_PLATE }}-tools

      - name: Run BCDK build
        run: |
          cd .pipeline
          ## npm install
          ## npm run build -- --pr=${{ env.PR_NUMBER }} --env=build 
